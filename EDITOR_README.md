# Sand Mosaic 关卡编辑器

## 打开

- **macOS（Apple Silicon）：双击同目录 `Start.command`，或在终端运行 `bash start.sh`。** 需要 arm64 Python 3.11–3.13；已有 Homebrew 可运行 `brew install python@3.12` 安装。首次启动联网安装 `requirements-runtime.txt` 中的依赖到 `.venv-macos/`，之后复用该环境。
- **Windows：双击同目录 `start.exe`。** 两个平台都无需安装 Node.js。等待界面底部显示「WebView 本地编辑模式」后编辑保存。普通浏览器只读，不能编辑保存。
- 原纯游戏入口保留为 `play.html`。

## 操作

左侧按主线优先 / 文件 ID 排序并搜索，关卡以 Canvas 缩略图分页显示；双击资源可查看或修改属性。

本地模式点击「进入编辑」：选择、移动（拖动松手定位）、放置选中预设、删除实体、连续画沙 / 擦沙。选择资源时自动切换放置或画沙工具。拖动绘制的一整笔只占一条撤销记录；取消拖动不会提交。沙粒画笔只在沙池内工作，不自动扩展沙池或修改盒子容量。

「矩形」工具：先选沙池、盒子、墙体或区域预设，再在画布拖出矩形。选中对象后，右侧的形状网格可单击增减格子，也可输入宽高设为矩形。缩小沙池会先提示移除的沙粒数量；玻璃或岩石超出新形状时须先调整机关。

沙池属性显示实际粒数、最大粒数与填充率。按颜色输入数量，选择分层或混合排列，点击「预览填充」，确认后「应用预览」。分层从池底向上按颜色编号排列，混合预览与应用使用相同结果。超量填充会报错，不会截断数量；也可选中色填满或清空。原沙粒编码兼容保留，修改后使用压缩的 GrainRuns 编码。新建默认密度为每格 18×18，已有沙池不改密度。

沙池内继续使用透明背景的色块与数量标签。「沙量」「网格」按钮可切换显示；画布顶部可放大至 150%、200%、300%。Ctrl/Cmd+Z 撤销，Ctrl/Cmd+Y 或 Ctrl/Cmd+Shift+Z 重做，Delete 删除选中对象，Esc 取消当前笔画并返回选择工具。

右侧「全局」包含高级配置；「关卡」包含名称、时间、棋盘尺寸、复制关卡和完整原始 JSON；「物品」使用中文表单编辑位置、颜色、容量、锁、冻结、移动限制等属性，完整对象 JSON 折叠到高级区。管道、多层、胶合组从实体列表选择；玻璃、岩石、隐藏对象和出盒队列在折叠列表中添加、删除、排序。编号及沙粒密度在表单中只读，避免意外破坏引用或填充数据。

复制对象会重新分配内部实体编号；复制盒子时复制其关联层级，复制沙池时复制其关联管道。删除沙池时提示并删除关联管道、修正后续管道索引；删除盒子时删除关联层级并更新胶合组。副本默认向右偏移一格，越界或重叠会在检查区提示，可继续移动调整。

「沙量与目标检查」按颜色对比池内和管道供应量、全部盒子目标容量（含多层、隐藏和队列），并提示重叠、越界、重复编号和部分失效引用。检查通过不等于已人工通关。所有编辑只改变内存；保存会先应用尚未应用的 JSON 草稿。

「开始试玩」使用独立的运行时，支持拖动、计时、机关解锁、暂停、重试和下一关。试玩不会写回编辑配置。

从编辑模式开始试玩，结束后返回编辑模式；沙池与盒子的初始配置保持不变。

右上角「保存」统一通过 `window.pywebview.api.files` 写入修改的文件。资源图片先在内存暂存，保存到 `level/asset/`。删除资源先检查 577 个工作关卡的引用；被引用的颜色索引或预设类型不可改名换键或删除。删除工作关卡需确认，保存时才删除文件。

## 文件

- `level/level-*.json`：577 个可编辑工作副本，初始内容与 Unity 原配置一致；文件命名符合技能约定。
- `levels/`：原始数据，不通过编辑器修改或删除。
- `gamelogic.js`：模型适配、校验、引用解析、Canvas 绘制、编辑工具、难度公式。
- `board.js`、`sand.js`：保留并复用之前的沙粒与机关运行时。
- `config.json`、`asset.json`：编辑器参数与资源映射；保存后的全局样式与规则用于编辑器试玩，原纯游戏入口仍按原 Unity catalog 加载。
- `levellist.js`：只负责发现命名规则及排序，不存关卡数组。
- `static/js/editor-store.js`：内存修改、撤销重做、统一保存与失败恢复。
- `editor-logic.js`：精确填沙、形状调整、复制删除关联及沙量检查。
- `static/js/visual-editor.js`、`static/css/visual-editor.css`：可视化属性表单与布局。

用户此前要求保留全部关卡，因此没有用模板单一示例替换 577 个关卡；「新建」通过 `createEmptyLevel` 生成一个最小可试玩样例，不擅自改已有 ID。

## 已知问题与验证范围

原始 ID 8 时间为 0、ID 257 无目标，校验报错；ID 568 沙量不足，校验提示。ID 396、400 各有重复占格，保留并提示。未编辑的异常文件不阻止其他文件保存。

测试命令：

```text
node verify-editor.mjs
node verify-editor-ui.mjs
node --test tests/editor-navigation.test.js
node --test tests/editor-operations.test.mjs
```

已验证 577 个副本哈希、Canvas 绘制调用、新建样例收沙通关、配置难度分数一致性、只读限制、延迟 WebView 接入、内存暂存、按改动保存、图像保存、资源引用保护、删除暂存、部分保存失败重试与未知字段往返。

另有 `tests/editor-preview.html` 浏览器界面测试页，以内存桥接模拟本地模式，页面明确标注「仅内存保存」，不会写入工作文件。已用浏览器验证填沙预览/应用、撤销/重做、矩形创建、连续画沙和内存保存状态，并检查布局；真实 `start.exe` 文件写入仍需在本地启动器验收。运行时出现桥接错误会在顶部展示代码与消息，并保留未保存状态。

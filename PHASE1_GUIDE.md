# 第一期：制作一关基础沙子关卡

## 打开与新建

1. 双击本目录 `start.exe`。底部应显示 **WebView 本地编辑模式**。普通浏览器仍然只读。
2. 点击左上角 **新建**，设置名称、棋盘宽高、限时和颜色数量。
3. 保持 **可试玩的基础布局**，点击 **创建并编辑**。编辑器会生成 1–3 组颜色匹配的沙池与盒子，沙量和容量相等。
4. 点击 **开始试玩**，把盒子移到同色沙池正下方。填满后消除，全部完成即过关。
5. 点击 **结束试玩** 回到原来的编辑状态，点击右上角 **保存**。新关卡写入 `level/level-<新ID>.json`。

也可以选 **空白棋盘** 从零画布局；空白棋盘需要添加至少一个盒子后才能保存与试玩。

## 常用操作

- **选择**：点击对象，右侧修改属性。盒子的颜色、收沙目标和移动限制都有中文表单。
- **移动**：拖动对象；预览绿色表示可放置，红色表示冲突。松手后吸附，非法移动不改变数据。
- **放置**：在资源库选择盒子或沙池，然后点击棋盘。资源库默认只显示基础工具和颜色。
- **矩形**：先选沙池、盒子或墙体预设，再在棋盘拖出矩形。
- **形状**：右侧可点击格子增减占地，也可选择单格、长条、方形、L/T 形，旋转或镜像。形状变化不会自动改变盒子容量。
- **按每格计算容量**：输入每格粒数，主动重新计算总容量。
- **复制对象**：普通盒子和沙池会寻找空闲位置；空间不足则保留原数据并提示。可在关卡属性里复制整关。
- **画墙 / 擦墙**：按住鼠标连续操作；擦墙只移除经过的墙格，一次笔画可整体撤销。
- **画沙 / 擦沙**：先选颜色，再在沙池里拖动操作。
- **填沙区**：先选颜色，再从一个沙池内部拖出矩形；只修改这个沙池范围内的沙粒。

## 批量配沙

选择沙池，在右侧 **沙量配置** 里填写各颜色的粒数。选择分层或混合排列，点击 **预览填充**，检查后点击 **应用预览**。分层按色表顺序从底部向上排列。

**选中色填满** 可以快速生成单色沙池的预览。**添加其他颜色** 中可以展开其它颜色。超出沙池可填数量会提示，不会改动当前数据。

缩小沙池如果会丢失沙粒，会先显示数量并要求确认；取消则保留全部沙子。含内部机关的沙池暂不支持旋转与镜像。

## 检查、撤销和保存

右侧 **沙量与目标检查** 显示每种颜色的供需和差额。缺沙应先处理；供需相等不代表布局一定可通关，需要试玩。

- `Ctrl+Z` 撤销，`Ctrl+Y` 或 `Ctrl+Shift+Z` 重做。
- `Esc` 取消尚未完成的画布手势。
- `Ctrl+S` 保存。所有其它编辑只修改内存。
- 关卡列表以 **未保存** 标记改动。保存失败时数据仍留在编辑器中。
- 保存与试玩会阻止新引入的基础布局越界和非法重叠。原文件已有的重复占格保留提示，不因这次升级阻止旧关卡试玩。

原始 `levels/` 目录和现有关卡文件不会因为升级而改写。特殊机关继续保留原有高级参数入口，第一期没有扩展它们的玩法。

## 验证范围

包含数据测试、原有模拟与编辑器回归、真实 Chrome 交互测试。浏览器交互测试使用独立的内存文件接口，验证保存内容与新文件不覆盖标记；实际 `start.exe` 系统文件桥接仍沿用原有接口，未将浏览器测试当作启动器实机写盘验证。
